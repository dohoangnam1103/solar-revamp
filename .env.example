# Database
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# App
NEXT_PUBLIC_SITE_URL="https://soliq.com.vn"
NEXT_PUBLIC_SITE_NAME="SOLIQ ENERGY"

# Admin (change this in production)
ADMIN_PASSWORD=""
ADMIN_SESSION_SECRET=""

# Analytics (optional)
NEXT_PUBLIC_GTM_ID=""
NEXT_PUBLIC_META_PIXEL_ID=""

# SMTP (for email notifications when a customer submits a form)
# Optional — if not set, email notifications are silently skipped.
# Example for Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587 (STARTTLS) or 465 (SSL).
# For Gmail, use an App Password (https://myaccount.google.com/apppasswords).
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM=""
